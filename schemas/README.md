# Schemas Domain

This directory contains the JSON Schemas describing the EKK Markdown format and the output format of `osef analyze --json`. These schemas are used by IDEs to provide autocomplete, and by other tools to parse OSEF outputs.
