# Installation

> [!WARNING]
> **Python 3.12+ Required**
> OSEF strictly enforces Python 3.12 or higher. Attempting to install on older versions will result in pip resolution errors. This is an architectural decision to leverage modern typing and asynchronous features.

```bash
pip install osef
```