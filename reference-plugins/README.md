# Plugins Domain

This directory is reserved for first-party OSEF plugins (e.g., `osef-fastapi`, `osef-security`). 

Third-party plugins can be developed anywhere and installed via `uv tool install osef --with my-plugin`.

*Note: Plugin development begins in Sprint 3.*
