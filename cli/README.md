# CLI Domain

This directory contains the Typer-based command-line interface presentation layer. 
The CLI contains **zero business logic**. It simply parses arguments, invokes the SDK, and renders output via the `Rich` library.

*Note: CLI implementation begins in Sprint 5.*
