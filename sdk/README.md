# SDK Domain

This directory will document and test the public Python SDK (`osef.init()`). 

The SDK allows you to embed OSEF's Transformation Engine and Knowledge Kernel directly into your own CI scripts or Python applications.

*Note: SDK implementation begins in Sprint 4.*
