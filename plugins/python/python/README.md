# OSEF Python Reference Platform

This is the canonical reference implementation of an OSEF Plugin.

It provides:
- Python parsing (via `ast`)
- Python semantic enrichment
- Python policy packs
- Python reports

## Requirements
- OSEF `^0.4.0`
